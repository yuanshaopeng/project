1、不划分分支流程
    git clone 远程仓库地址      将gitHub远程仓库代码克隆到本地（只在项目刚开启时执行  一次）；
   
          编写代码。。。。
    git status  查看本地仓库的文件状态   将标红的文件提交
    git add 文件名    将文件提交到缓冲池
    git commit -m "注释"   将缓冲池内的文件生成为版本；
	git push    将版本区代码推入远程仓库  //如果远程仓库版本与本地仓库版本不统一  会造成提交失败
	第二天
	git pull   拉取远程仓库最新版本
	